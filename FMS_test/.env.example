DATABASE_URL=postgresql://user:password@host:5432/dbname

# v22 테이블이 생성된 스키마 이름 (기본값: smartcast).
DB_SCHEMA=smartcast

# AWS RDS SSL 인증서 경로 (global-bundle.pem).
# 비워두면 ../global-bundle.pem 을 자동으로 탐색합니다.
SSL_CERT=
