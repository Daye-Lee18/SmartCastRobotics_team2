DATABASE_URL=postgresql://user:password@host:5432/dbname

# 스키마 이름 (기본값: smartcast).
DB_SCHEMA=smartcast

# AWS RDS SSL 인증서 경로 (global-bundle.pem).
# 비워두면 SmartCastRobotics_team2/global-bundle.pem 을 자동으로 탐색합니다.
SSL_CERT=
